<template>
  <div class="login-container">
    <div class="login-card">
      <!-- Logo Section -->
      <div class="logo-section">
        <img :src="logoPath" alt="Planning Logo" class="logo-image" />
      </div>

      <!-- Buttons Section -->
      <div class="buttons-section">
        <!-- Primary Button -->
        <button class="btn btn-primary" @click="handleThermondoLogin">
          <div class="btn-content">
            <div class="btn-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
                <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
                <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/>
                <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
              </svg>
            </div>
            <span class="btn-text">Mit thermondo-Konto anmelden</span>
          </div>
        </button>

        <!-- Secondary Button -->
        <button class="btn btn-secondary" @click="handleSalesforceLogin">
          <div class="btn-content">
            <span class="btn-text">Mit Salesforce-Konto anmelden</span>
          </div>
        </button>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  variant?: 'TC' | 'EC'
}

const props = withDefaults(defineProps<Props>(), {
  variant: 'TC'
})

// Get logo path for public assets
// In Vite, public assets are served at root, but we need to respect BASE_URL for production
const baseUrl = import.meta.env.BASE_URL
// Ensure we have proper path: baseUrl ends with / or we add it
const logoPath = baseUrl.endsWith('/') 
  ? `${baseUrl}Logo_planning.png`
  : `${baseUrl}/Logo_planning.png`

const handleThermondoLogin = () => {
  // Mock handler for prototype
  console.log(`Thermondo login clicked for variant: ${props.variant}`)
  // In production, this would trigger SSO flow
}

const handleSalesforceLogin = () => {
  // Mock handler for prototype
  console.log(`Salesforce login clicked for variant: ${props.variant}`)
  // In production, this would trigger SSO flow
}
</script>

<style scoped>
.login-container {
  width: 100%;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-background, #f5f3ef);
  padding: 20px;
}

.login-card {
  width: 322px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.logo-section {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: var(--spacing-xl);
}

.logo-image {
  max-width: 100%;
  height: auto;
  object-fit: contain;
}

.buttons-section {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
}

.btn {
  width: 100%;
  height: 48px;
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  border: none;
  transition: opacity 0.2s;
}

.btn:hover {
  opacity: 0.9;
}

.btn:active {
  opacity: 0.8;
}

.btn-primary {
  background-color: var(--color-primary, #046df6);
}

.btn-secondary {
  background-color: var(--color-white, #ffffff);
  border: 1px solid var(--color-border, #ededed);
}

.btn-content {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-sm) var(--spacing-md);
}

.btn-icon {
  display: flex;
  align-items: center;
  padding: 4px;
  width: 24px;
  height: 24px;
}

.btn-text {
  font-family: var(--font-family-body);
  font-weight: var(--font-weight-bold);
  font-size: var(--font-size-base);
  line-height: var(--line-height-button);
  white-space: nowrap;
}

.btn-primary .btn-text {
  color: var(--color-white, #ffffff);
}

.btn-secondary .btn-text {
  color: var(--color-text, #1a1a1a);
}

@media (max-width: 480px) {
  .login-card {
    width: 100%;
    max-width: 322px;
  }
}
</style>
